"""Evaluation harness for RegimeVGGT.

Each ``eval_*.py`` is a standalone script that takes dataset paths via CLI
flags and produces ``results/<method>/<dataset>/{logs.txt, summary.json}``
under the ``--output_dir`` (or ``--output_path``).
"""
